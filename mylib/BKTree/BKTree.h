#include "../../include/core.h"

typedef struct treenode* BK_treenode;
typedef struct tree* BK_tree;

enum ErrorCode build_entry_index(const entry_ist* el, enum MatchType type, index* ix)
int compare_words(const char* word1, const char* word2);
enum ErrorCode destroy_entry_index(index* ix)
